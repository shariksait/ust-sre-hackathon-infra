
name: Comment on Pull Request

on:
  pull_request:
    types: [opened]

permissions:
  pull-requests: write  # ✅ Needed to comment on PRs
  contents: read

jobs:
  comment:
    runs-on: ubuntu-latest

    steps:
      - name: Comment on the PR
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: `### 🚨 Incident Alert: Potential Recurrence Detected
                    👋 A potential recurrence of **Incident [INC-2478](https://docs.google.com/document/d/1bO7VnWskuSD8p9caEsne
                     > Missing subnet configurations. 
                     ---
                    #### 📄 RCA Document
                     [View RCA Document](https://docs.google.com/document/d/1bO7VnWskuSD8p9caEsnYoeB_yToXPaD185rPo8ZqVVI/edit?ction.`
            })
